@props(['header' => null])

@php
    $user = auth()->user();
    $isAdmin = $user?->hasRole(['admin','super-admin']);
@endphp

<header class="sticky top-0 z-40">
    <div class="px-4 sm:px-6 pt-4">
        <div x-data="{ mobileMenu:false }" class="bg-white/80 backdrop-blur border border-slate-200 shadow-sm rounded-3xl overflow-hidden">

            <!-- Row 1 -->
            <div class="h-16 px-4 sm:px-6 flex items-center justify-between gap-3">

                <!-- Brand -->
                <div class="flex items-center gap-3 min-w-0">
                    <div class="w-11 h-11 rounded-2xl bg-emerald-600 text-white flex items-center justify-center font-black shadow-sm">
                        ق
                    </div>
                    <div class="min-w-0">
                        <div class="font-extrabold leading-5 truncate">مركز التحفيظ</div>
                        <div class="text-xs text-slate-500 truncate">{{ $header ?? 'لوحة التحكم' }}</div>
                    </div>
                </div>

                <!-- Desktop Nav -->
                <nav class="hidden lg:flex items-center gap-2">
                    <a href="{{  route('dashboard') }}"
                    class="px-4 py-2 rounded-2xl text-sm font-semibold transition }">    📊 الرئيسية  </a>

                    <a href="{{ route('hifdh.index') }}"
                       class="px-4 py-2 rounded-2xl text-sm font-semibold transition }">   📖 تسجيل التسميع  </a>

                    <a href="{{ route('absences.index') }}"
                           class="px-4 py-2 rounded-2xl text-sm font-semibold transition">  🧾 الغياب  </a>
              
         
                        <a href="{{ route('students.index') }}"
                           class="px-4 py-2 rounded-2xl text-sm font-semibold transition ">  👥 الطلاب </a>
@if($isAdmin)

               <a href="{{ route('halaqas.index') }}"
                           class="px-4 py-2 rounded-2xl text-sm font-semibold transition">  🕌 الحلقات  </a>

                         <a href="{{ route('muhafidhs.index') }}"
                           class="px-4 py-2 rounded-2xl text-sm font-semibold transition "> 👤 المحفظون</a>
                  @endif
               
                </nav>

                <!-- Right actions -->
                <div class="flex items-center gap-2">
                    <span class="hidden sm:inline text-xs text-slate-500">
                        {{ now()->format('Y-m-d') }}
                    </span>

                    <div class="hidden md:flex items-center gap-2 px-3 py-2 rounded-2xl bg-slate-50 border border-slate-200">
                        <span class="text-sm">👤</span>
                        <div class="leading-4">
                            <div class="text-xs font-bold text-slate-800 truncate max-w-[160px]">
                                {{ auth()->user()->name ?? '' }}
                            </div>
                            <div class="text-[11px] text-slate-500">
                                {{ $isAdmin ? 'Admin' : 'Muhafidh' }}
                            </div>
                        </div>
                    </div>

                    <!-- Logout (Fixed) -->
                    <form method="POST" action="{{ route('logout') }}" class="hidden sm:block">
                        @csrf
                        <button type="submit"
                                class="px-4 py-2 rounded-2xl bg-slate-900 text-white text-sm font-semibold hover:bg-slate-800 transition">
                            خروج
                        </button>
                    </form>

                    <!-- Mobile menu button -->
                    <button type="button"
                            class="lg:hidden p-2 rounded-2xl hover:bg-slate-100"
                            @click="mobileMenu = !mobileMenu"
                            aria-label="menu">
                        ☰
                    </button>
                </div>
            </div>

  <!-- Mobile dropdown -->
<div x-show="mobileMenu" x-transition.opacity class="lg:hidden border-t border-slate-200 p-3">
    <div class="space-y-2">

        <a href="{{ route('dashboard') }}"
           class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition ">   <span>📊</span> الرئيسية </a>

        <a href="{{ route('hifdh.index') }}"
           class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition ">   <span>📖</span> تسجيل التسميع </a>

        <a href="{{ route('absences.index') }}"
           class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition ">    <span>🧾</span> الغياب </a>        

        <a href="{{ route('students.index') }}"
           class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition">   <span>👥</span> الطلاب </a>
        
        @if($isAdmin)
            <a href="{{ route('halaqas.index') }}"
               class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition "> <span>🕌</span> الحلقات  </a>

            <a href="{{ route('muhafidhs.index') }}"
               class="flex items-center gap-2 px-4 py-3 rounded-2xl text-sm font-semibold transition ">   <span>👤</span> المحفظون  </a>
            
        
        @endif

        <div class="pt-2 border-t border-slate-200">
            <div class="flex items-center justify-between gap-3 px-2">
                <div class="text-xs text-slate-500">
                    <div class="font-bold text-slate-800 truncate max-w-[220px]">
                        {{ auth()->user()->name ?? '' }}
                    </div>
                    <div class="text-[11px] text-slate-500">
                        {{ $isAdmin ? 'Admin' : 'Muhafidh' }}
                    </div>
                </div>

                <form method="POST" action="{{ route('logout') }}">
                    @csrf
                    <button type="submit"
                            class="px-4 py-2 rounded-2xl bg-slate-900 text-white text-sm font-semibold hover:bg-slate-800 transition">
                        خروج
                    </button>
                </form>
            </div>
        </div>

    </div>
</div>

        </div>
    </div>
</header>
